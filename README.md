# estiloyglam

Productos: el catálogo se carga desde `data/productos.json`. Puedes editar ese archivo para actualizar el inventario (nombre, precio, descripción, imagen).

CSS: los estilos están en `css/styles.css`.